import { CheckDataPipe } from './check-data.pipe';

describe('CheckDataPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckDataPipe();
    expect(pipe).toBeTruthy();
  });
});
